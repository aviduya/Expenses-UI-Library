import XCTest
@testable import Expenses_UI_Library

final class Expenses_UI_LibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Expenses_UI_Library().text, "Hello, World!")
    }
}
